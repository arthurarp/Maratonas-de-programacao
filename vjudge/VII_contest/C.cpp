#include <bits/stdc++.h>

using namespace std;

int main(void)
{
    int n; cin >> n;
    string s; cin >> s;
    int total = 0;

    for(int i = 0; i < n - 1; ++i)
    {
        if(s[i] == '2' or s[i] == '4' or s[i] == '6' or s[i] == '8')
            total++;
        for(int j = i + 1; j < n; ++j)
        {
            if(s[i] )
        }
    }

   

    return 0;
}