#include <bits/stdc++.h>

using namespace std;

int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(0);

    int n; cin >> n;

    int rooms_count = 0;

    while (n--) {
        int p, q; cin >> p >> q;

        if (q - p >= 2)
          rooms_count++;

    
    
    
    
    }

    cout << rooms_count << endl;


  return 0;
}
